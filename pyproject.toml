[build-system]
requires = ["setuptools>=64.0", "setuptools-scm>=8.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "CausalEstimate"
dynamic = ["version"]
description = "Pandas-native causal effect estimation with IPW, AIPW, TMLE, matching, and diagnostics."
readme = { file = "README.md", content-type = "text/markdown" }  # Combine file and content-type
authors = [
    { name = "Kiril Klein", email = "kikl@di.ku.dk" },
]
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "scipy",
    "statsmodels",
    "tqdm"

]

[project.optional-dependencies]
dev = [
    "pytest",
    "black==26.5.1",
    "flake8",
    # Add other development dependencies here
]
plotting = [
    "matplotlib",
]
docs = [
    "mkdocs-material",
    "mkdocstrings[python]",
]
[tool.setuptools_scm]
write_to = "CausalEstimate/_version.py"
[tool.setuptools.packages.find]
include = ["CausalEstimate*"]
exclude = ["notebooks", "tests*"]
# Add your project URLs here
[project.urls]
"Source" = "https://github.com/kirilklein/CausalEstimate.git"
"Documentation" = "https://kirilklein.github.io/CausalEstimate/"

[tool.black]
target-version = ["py311"]
